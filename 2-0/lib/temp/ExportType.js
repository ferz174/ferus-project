if (nax) {
	if (nuse){
		
	} else {
		
	}
} else {
	
}

//if (!ExportType) {
	
} else if (ExportType == 1) {
	
} else if (ExportType == 2) {
	
} else if (ExportType == 3) {
	
}

OutExport = false;

if (!ExportType && RepositoryHTML.indexOf("/0/") {
	if (nax) Repository = "/"+UserID+"/";
	Repository = "/1/";
	ExportType = 2;
} else if (RepositoryHTML.indexOf("/"+XS_PERF+"/") {
	Repository = "/"+XS_PERF+"/";
	
	
} else if (RepositoryHTML.indexOf("/"+UserID+"/") {
	Repository = "/"+UserID+"/";
} else if (RepositoryHTML.indexOf("/"+UserName+"/") {
	Repository = "/1/";
} else if (!ApplyIn && typeof(RepositoryID) != "undefined" && RepositoryID) {
	OutExport = true;
	Repository = (typeof(RepositoryID.getElementsByTagName("i")[0].textContent) != "undefined") ? RepositoryID.getElementsByTagName("i")[0].textContent : RepositoryID.getElementsByTagName("i")[0].innerText;
} else {
	OutExport = true;
	Repository = RepositoryHTML;
}

if (ExportType && OutExport) OutExport = GetFindFile(PathExport, PathExport, Reports+Repository+Report.File);
if (OutExport && !EventError.hasOwnProperty("–епозитори€ есть в Ёкспорте!")) EventError["–епозитори€ есть в Ёкспорте!"] = OutExport;